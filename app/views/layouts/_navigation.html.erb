<nav class="navbar navbar-dark navbar-static-top navbar-expand-md">
    <div class="container">
        <button type="button" class="navbar-toggler collapsed" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1" aria-expanded="false"> <span class="sr-only">Toggle navigation</span>
        </button> <%= link_to "E-SHOP", root_path, class: 'navbar-brand' %>
        
        <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
            <ul class="nav navbar-nav mr-auto">
              <li class='nav-item'><%= link_to 'Shop', products_path, class: 'nav-link' %></li>
            </ul>
        </div>

        <div class=" my-2">
            <% if current_user %>
                <%= link_to "Log out", destroy_user_session_path, method: "delete" %> |
                <%= link_to 'Dashboard', dashboard_path %>
                <% link_to "Log out Admin", destroy_admin_user_session_path, method: "delete" %>
               
            <% else %>
                <%= link_to 'Sign in', new_user_session_path %>
                <%= link_to 'Register', new_user_registration_path %>
            <% end %>
            <%= link_to ('<i class="fa fa-shopping-basket"></i>').html_safe, cart_path(@cart) %>
             <% # link_to text_with_badge('<i class="fa fa-shopping-basket"></i>', @cart.cart_items.count), cart_items_path, class: 'btn btn-primary', data: {toggle: :tab} %>
        </div>
    </div>
</nav>